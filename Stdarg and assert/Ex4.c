#include <stdio.h>
#include <assert.h>

int main()
{
    int b;
    scanf("%d", &b);
    if(b == 0)
    {
        printf("yes\n");
    }
    else 
    {
        printf("no\n");
    }
}
